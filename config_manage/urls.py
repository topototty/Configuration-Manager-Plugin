# For currently unclear reasons, this import is needed for the tab view to work
from .views import DeviceConnectionView

# The URL patterns are empty as we don't have any independently accessible
# views in this plugin
urlpatterns = []
